package character;
/**
 * Defines what is needed for an NPC character
 */
public abstract class NPC extends Character{ //nothing special just for NPCs yet
    public NPC(String name, String description) {
        super(name, description);
    }

}
